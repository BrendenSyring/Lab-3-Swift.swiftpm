import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello world from Brenden!")
                .font(.system(size:36, weight: .bold, design: .rounded))
                .foregroundColor(.purple)
                
        }
    }
}
